# Clewseau preset

Appends Clewseau's durable-ID rules onto Spec Kit core templates via `append` strategy. Does not replace Spec Kit's workflow.

Install:

```bash
specify preset add --dev /path/to/clewseau/presets/clewseau
```

See the repo root [`PROMOTION-CONTRACT.md`](../../PROMOTION-CONTRACT.md).
